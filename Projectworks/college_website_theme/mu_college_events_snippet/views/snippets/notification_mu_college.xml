<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="notification_mu_college">
        <t t-call-assets="website.assets_frontend"/>
        <t t-set="_cp" t-value="_cp or json.loads(_record.cover_properties)"/>
        <t t-set="_name" t-value="_name or _record._name"/>
        <t t-set="_id" t-value="_id or _record.id"/>
        <t t-set="_bg" t-value="_bg or _record._get_background(height=_resize_height, width=_resize_width)"/>
        <t t-set="default_cover_name">Cover</t>
        <div t-att-data-name="display_opt_name or default_cover_name"
             t-att-style="_cp.get('background_color_style')"
             t-att-data-use_size="use_size"
             t-att-data-use_filters="use_filters"
             t-att-data-use_text_align="use_text_align"
             t-att-data-res-model="request.env.user.has_group('website.group_website_publisher') and _name"
             t-att-data-res-id="request.env.user.has_group('website.group_website_publisher') and _id"
             t-attf-class="o_record_cover_container  #{_cp.get('background_color_class')} #{use_size and _cp.get('resize_class')} #{use_text_align and _cp.get('text_align_class')} #{additionnal_classes}">
            <section class="notification" style="background-image: none;">
                <h1 class="notification-title" t-field="record.name"/>
                <div class="notification-p" t-field="record.event_description"/>
                <div class="bottom_notify">
                    <div class="notification_date">
                        <t t-set="formatted_date" t-value="record.post_date.strftime('%d/%b/%Y')"/>
                        <span t-esc="formatted_date"/>
                    </div>
                    <a t-att-href="data['call_to_action_url']" class="notification_read">
                        Read More
                    </a>
                </div>

            </section>
            <style>
                .notification-title{
                color: #000 !Important;
                font-family: Inter;
                font-size: 16px;
                font-style: normal;
                font-weight: 600;
                line-height: 28px;
                margin:0;
                }
                .notification-p{
                color: #383838;
                text-align: justify;
                font-family: Urbanist;
                font-size: 16px;
                font-style: normal;
                font-weight: 500;
                line-height: 26px;
                margin:0;
                }
                .notification{
                padding:14px;
                padding-bottom:0;
                border: 1px solid #F4F9F4;
                background: #FFF;
                }
                .notification_date{
                color: #000;
                font-family: Inter;
                font-size: 14px;
                font-style: normal;
                font-weight: 500;
                line-height: 28px;
                }
                .notification_date span{
                font-family: Inter;
                font-size: 14px;
                font-style: normal;
                font-weight: 500;
                line-height: 28px;
                }
                .bottom_notify{
                height:28px;
                width:100%;
                display:flex;
                justify-content:space-between;
                }
                .notification_read{
                color: #141F7E !important;
                font-family: Inter;
                font-size: 14px;
                font-style: normal;
                font-weight: 500;
                line-height: 28px;
                display:block;
                }
                .notifications .s_blog_post_mu_college_notification .row {
                animation: moveUp 20s linear infinite;
                overflow: auto;
                gap: 10px;
                }
                .notifications .container{
                padding:0;
                }
                .s_blog_post_mu_college_notification .col-3 {
                width: 100%;
                }
                @keyframes moveUp {
                0% {
                transform: translateY(10%);
                }
                100% {
                transform: translateY(-100%);
                }
                }
                .notifications .s_blog_post_mu_college_notification .row {
                animation-play-state: paused;
                }

            </style>
            <div t-if="is_sample" class="h5 o_ribbon_right bg-primary text-uppercase">Sample</div>
            <style>

            </style>
        </div>
    </template>
    <template id="dynamic_filter_template_blog_post_mu_college_notification" name="notifications">
        <div t-foreach="records" t-as="data" class=" w-100 s_blog_posts_post notification_mu_college">
            <t t-set="record" t-value="data['_record']"/>
            <div class="s_blog_posts_post_cover_notification_mu_college">
                <t t-call="mu_college_events_snippet.notification_mu_college">
                    <t t-set="_record" t-value="record"/>
                    <t t-set="use_filters" t-value="True"/>
                    <t t-set="additionnal_classes" t-value="' '"/>
                </t>
            </div>
        </div>
    </template>
</odoo>
